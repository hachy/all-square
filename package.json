{
  "name": "all-square",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf ./.cache",
    "dev": "npx parcel src/index.html --open",
    "build": "npm run clean && npx parcel build src/index.html --no-source-maps --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hachy/all-square.git"
  },
  "author": "hachy",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hachy/all-square/issues"
  },
  "homepage": "https://hachy.github.io/all-square/",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.9.7"
  }
}
